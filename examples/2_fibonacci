fn fibonacci (max: int) int[] {
  mut result := [0, 1]

  loop i in 2 ..= max {
    result.push(result[i - 2] + result[i - 1])
  }

  return result
}

main {
  print("Fibonacci sequence:", fibonacci(50))
}
